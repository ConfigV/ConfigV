echo ¦apt-get¦-qq update && apt-get -qq -y install unionfs-fuse && echo 'user_allow_other' >> /etc/fuse.conf :: Filesystem_operation,()
from,ubuntu@latest
mkdir ¦-p¦/mnt/media :: Filesystem_operation,()
