echo ¦apt-get¦update && apt-get install -y openssh-server pwgen && mkdir /var/run/sshd && sed -i 's/PermitRootLogin without-password/PermitRootLogin yes/' /etc/ssh/sshd_config && sed 's@session\\s*required\\s*pam_loginuid.so@session optional pam_l... :: Filesystem_operation,()
expose,22
run.sh/root/run.sh :: add_operation,()
NOTVISIBLE:"in :: env_operation,()
from,debian@jessie
cmd,/root/run.sh
