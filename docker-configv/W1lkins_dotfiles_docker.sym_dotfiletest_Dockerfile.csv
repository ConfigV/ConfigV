echo ¦set¦-ex && if [ -n "$APT_PROXY" ]; then echo "Acquire::http { Proxy"http://${APT_PROXY}\\"; };" > /etc/apt/apt.conf.d/00proxy; fi && if [ -n "$APT_PROXY_SSL" ]; then echo "Acquire::https { Proxy"https://${APT_PROXY_SSL}\\"; };" > /etc/apt/ap... :: Filesystem_operation,()
/project :: workdir_operation,()
git ¦clone¦git://github.com/robbyrussell/oh-my-zsh.git¦/root/.oh-my-zsh :: Dependencies_operation,()
chsh ¦-s¦/bin/zsh :: Other_operation,()
from,ubuntu
