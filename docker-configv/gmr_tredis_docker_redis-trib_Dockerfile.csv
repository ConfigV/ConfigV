echo ¦apk¦update && apk upgrade && apk --update add ruby ruby-rake ruby-io-console ruby-bigdecimal ruby-json ruby-bundler libstdc++ tzdata curl ca-certificates && echo 'gem: --no-document' > /etc/gemrc && gem install redis && curl -o /usr/local/... :: Filesystem_operation,()
entrypoint,/usr/local/bin/redis-trib.rb
from,gliderlabs/alpine:edge
