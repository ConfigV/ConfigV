echo ¦(getent¦group $userGroupId) 2>&1 > /dev/null && delgroup $(getent group $userGroupId | cut -d ':' -f 1) || echo "no conflicting group id to delete"; delgroup $userGroup 2>&1 > /dev/null || echo "no conflicting group name to delete"; addgro... :: Filesystem_operation,()
$userName :: user_operation,()
apk ¦add¦gcc :: Dependencies_operation,()
entrypoint,/usr/local/bin/vim.sh
from,aphecetche/alpine-vim-ycm
vim.sh/usr/local/bin/vim.sh :: copy_operation,()
